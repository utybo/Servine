plugins {
    id 'guru.zoroark.servine.kotlin-library-conventions'
}

dependencies {
    api project(':redirects-parser')
    api libs.ktor.server.core

    testImplementation libs.ktor.server.testHost
    testImplementation libs.jimfs
}
